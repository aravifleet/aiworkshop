# prototype2

This workspace contains an agentic QA framework for developer instruction-driven automation.

The framework reads instruction files from `/docsnew`, generates use cases, executes Playwright automation in POM style, and stores outputs under `/results`, `/reports`, `/screenshots`, `/dom/dom_elements`, `/usecases`, and `/playwright-script`.
