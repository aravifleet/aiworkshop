We recently developed an instance - https://practicetestautomation.com/practice-test-login/, we require a responsive testing, checking all available links - doing all possible actions, ecommerce checkout, clicking all links and filling all possible forms, if require use the below credentials if the operation requires including login logout.

For every run, capture the DOM freshly at the start and delete any previously stored DOM structure before proceeding. Do not reuse an older DOM snapshot or cached structure from a previous run. This is required so any site changes are picked up newly each time instead of the process getting stuck on an old structure.

Usecases needed only 10

credentials :

email : student
password : Password123
